@page
@model LoginWithRecoveryCodeModel
@{
    ViewData["Title"] = "備用碼驗證";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card shadow border-warning">
                <div class="card-header bg-warning text-dark text-center">
                    <h4 class="mb-0">@ViewData["Title"]</h4>
                </div>
                <div class="card-body p-4">
                    <div class="alert alert-light border text-muted small mb-4">
                        <i class="bi bi-info-circle-fill"></i>
                        您正在使用備用碼登入。在您重新設定驗證器應用程式之前，系統將不會記住此瀏覽器的登入狀態。
                    </div>

                    <form method="post">
                        <div asp-validation-summary="ModelOnly" class="text-danger mb-3" role="alert"></div>

                        <div class="form-floating mb-3">
                            <input asp-for="Input.RecoveryCode" class="form-control" autocomplete="off" placeholder="請輸入備用碼" />
                            <label asp-for="Input.RecoveryCode">請輸入備用碼</label>
                            <span asp-validation-for="Input.RecoveryCode" class="text-danger"></span>
                        </div>

                        <div class="d-grid gap-2">
                            <button type="submit" class="btn btn-lg btn-warning">驗證登入</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}